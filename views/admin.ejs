<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
</head>
<body>
    <h1>Bienvenue, <%= user.username %>!</h1>
    <h2>Liste des utilisateurs :</h2>
    <ul>
      <% users.forEach(u => { %>
        <li><strong><%= u.username %></strong> - <%= u.email %> - <%= u.role %></li>
      <% }) %>
    </ul>
</body>
</html>
